<% if mylist == nil %> 
<% else %>
    <%= form_with model:list, url:destroy_mylist_path(list.collection_id, list.track_id), method: :delete, remote: true do |mylist| %>

        <%= mylist.submit 'MyListからはずす' %>
    <% end %>
<% end %>
